# 検証レポート: フルスタック化 (Docker + DB)

## 実装内容
アプリケーションのデータ保存先を、ブラウザのローカルストレージから、サーバー上のデータベース（PostgreSQL）に移行しました。

### 1. 🏗️ インフラ構成
- **Docker**: データベース環境をコンテナ化しました。
- **PostgreSQL**: 堅牢なリレーショナルデータベースを採用しました。
- **Prisma**: データベース操作を安全かつ簡単に行うためのORMを導入しました。

### 2. 🔌 バックエンド
- **Server Actions**: `src/actions/stamp.ts` にデータベース操作ロジックを集約しました。
    - `getStamps(userId)`: ユーザーのスタンプ一覧を取得
    - `toggleStamp(userId, date)`: スタンプのON/OFFを切り替え

### 3. 👤 ユーザー管理
- **匿名認証**: 初回アクセス時に自動的に「ユーザーID」を発行し、ブラウザに保存します。
- これにより、ログインの手間なく、かつデータをサーバー側で安全に管理できるようになりました。

### 注意点
- **データの引継ぎについて**: 今回の変更により、以前のローカルストレージのみで保存されていたスタンプはいったん表示されなくなります（新規IDが発行されるため）。
- 以前のデータは消えていません。もし必要であれば「データ移行機能」を追加することも可能です。

## 検証方法
1. **DBの起動**: `docker-compose up -d` でコンテナが動いていること。
2. **スタンプ動作**: カレンダーの日付をクリックして、リロードしてもスタンプが維持されること。
3. **データ確認**: `npx prisma studio` などのツールで、実際にDBにデータが入っていることが確認可能。
